Use ".let" to define Haskell one-line functions (e.g. ".let square x = x * x).   Use ".eval" to evaluate expressions.  For  a complete list of commands, type ".info".  We welcome feedback (email admin@lembrary.com).
